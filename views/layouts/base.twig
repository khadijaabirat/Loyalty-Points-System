<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ShopEasy{% endblock %}</title>
    <link rel="stylesheet" href="/Loyalty_Points_System/public/css/style.css">
    {% block stylesheets %}{% endblock %}
</head>
<body>
<header class="header">
    <div class="container">
        <a href="/Loyalty_Points_System/public/shop" class="logo">ShopEasy</a>
        <nav class="nav">
            {% if user %}
                <span class="user-info">
                    <strong>{{ user.name }}</strong>
                    <span class="points-badge" title="Votre solde de points">
                        ⭐ {{  user.total_points|default(0) }} points
                    </span>
                </span>
                <a href="/Loyalty_Points_System/public/logout" class="cart-link" style="margin-left: 10px;">Déconnexion</a>
            {% else %}
                <a href="/Loyalty_Points_System/public/login">Connexion</a>
            {% endif %}

            <a href="/Loyalty_Points_System/public/shop/cart" class="cart-link">
                Panier
                {% if cart_count > 0 %}
                    <span class="cart-count">{{ cart_count }}</span>
                {% endif %}
            </a>
        </nav>
    </div>
</header>

    <main class="main">
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2026 ShopEasy - Système de test pour formation MVC</p>
            {% block footer_extra %}
                <a href="/shop/reset" class="reset-link">Réinitialiser la session</a>
            {% endblock %}
        </div>
    </footer>

    {% block javascripts %}{% endblock %}
</body>
</html>